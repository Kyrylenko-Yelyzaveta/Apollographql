query  CountryQuery($country_code: ID!){
    country(code: $country_code){
        name
        capital
        emoji
        currency
        languages {
            name
        }
        continent {
            name
        }
    }
}